import { SystemMessages } from '@/shared/types/utils';

export const baseRemoveTitle = 'removing';

export const baseRemoveMessages: SystemMessages = {
	success: 'removedSuccessfully',
	fail: 'removedFailed'
};
